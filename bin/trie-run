#!/usr/bin/env bash
set -euo pipefail

task_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)
PYTHONPATH="$task_root/src${PYTHONPATH:+:$PYTHONPATH}" \
  exec python3 -m trie_remote local "$@"

