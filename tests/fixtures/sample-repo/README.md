# Remote runner fixture

This directory is the tracked seed for dirty-worktree synchronization smoke tests. Tests may copy it to a temporary Git repository, then add modified, deleted, and untracked files without changing this source fixture.
